/**
 * 双栈: 为了节省空间, 让两个数据元素类型一致的栈共享一维数主空间
 */

// ==================== 定义 ====================
/**
 * 两个栈栈底分别设在数组两端, 两栈彼此迎面"增长"
 */
const int max = 40;
typedef struct DbStack
{
    DataType data[max];
    int top1, top2;
} DoubleStack;

// ==================== 运算 ====================