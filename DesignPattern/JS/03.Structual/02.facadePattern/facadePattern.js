/**
 * use facade to:
 *   provide a simplified interface to a complicated system
 *   for example: jQuery is facade to DOM
 *
 * facade VS decrator
 *   they are both wrapping something
 *   bug facade wrap to simplify
 *   decrator wrap to extend funtionality
 */