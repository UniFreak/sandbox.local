/**
 * Command Pattern:
 * encapsulates the calling of a method as an object, so you can:
 * - fully decouple the execution from the implementation
 * - allows for fragile implementations
 * - support undo operation
 * - support auditing and logging of opperations
 */