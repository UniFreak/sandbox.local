/**
 * prepend 0o or 0O to convert into binary value
 */
let oValue = 0o10;
console.log(oValue);

/**
 * prepend 0b or 0B to convert into binary value
 */
let bValue = 0b10;
console.log(bValue);